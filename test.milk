[preset00]
per_frame_1=wave_r = 0.5 + 0.5*sin(time);wave_g = if(above(bass, 1.2), 1, 0.5);wave_b = sqr(mid);my_user_var = 1.0;
per_pixel_1=rot = if(equal(my_user_var, 1.0), 0.1, 0.0);r = my_user_var * x;g = wave_g * y;b = if(below(rad,0.1),1,0);